# 第三次人工智能编程作业

## 1.任务拆解和AI协作策略

将六个独立任务模块依次拆分给AI完成，并且依次向AI提出具体要求：

1. **数据预处理**：要求 AI 使用 pandas 读取制表符分隔文件、解析时间、计算 ride_stops 并删除异常记录。
2. **时间分布分析**：强调必须用 numpy 布尔索引统计早晚时段，并要求用 matplotlib 直接绘制柱状图并区分颜色。
3. **线路站点分析**：要求 AI 严格按照给定函数签名编写 `analyze_route_stops`，并绘制 seaborn 水平条形图。
4. **高峰小时系数**：引导 AI 自动识别高峰小时，再用 `resample` 进行5/15分钟聚合，按公式计算 PHF。
5. **文件批量导出**：要求 AI 筛选线路、创建文件夹、输出去重后的车辆-驾驶员对应关系。
6. **服务绩效热力图**：要求 AI 计算四个维度的 Top10，构造矩阵并用 seaborn.heatmap 绘图。

每个任务都对代码和AI生成的代码进行调试，并在发现错误后进行修正，或者对AI提出进一步的指令再进行修改

## 2.核心prompt迭代记录

### 事例1：Task2 可视化不清晰

**存在问题**
**初代 Prompt**：
> 首次提问：“用 matplotlib 画一个24小时刷卡量柱状图，x轴小时，y轴刷卡量。”

**AI 生成的问题**：
- 运行AI所生成的程序后发现图片中的柱子颜色都是相同的，早峰前和深夜时段没有被单独区分。
- 代码中使用了 seaborn 的 `barplot` 而非题目要求的直接 matplotlib，调用了错误的库。

**优化后的 Prompt**：
> 再向AI提问的过程中加上了对间隔、早晚区分、中文标题的要求“只能使用 matplotlib库 绘制24小时刷卡量柱状图。其中x轴为小时0~23，步长2。早上h<7和晚上h>22 的时间橙色高亮，其余时段用蓝色。保存的文件名为"hour_distribution.png"”

**结果**：AI 生成了完全符合要求的代码，包含颜色列表循环判断和图例。

### 事例2：Task3 可视化要求出现问题
**存在问题**
**初代prompt**：
>首次提问：“依据前面对平均站点数目前15的线路提出进一步进行可视化，给出横向水平图表”

**AI生成的问题**：
- 在水平条形图的绘制中没有使用seaborn的barplot语句，而是使用matplotlib语句绘制图像
- 没有按照水平条形图的格式要求——没有绘制误差棒，没有使用渐变色
- 保存格式错误，dpi未进行设置

**优化后的prompt**
>再次向AI提问的时候，要求严格使用seaborn，绘制出误差棒并且使用seaborn中的palette调色板进行渐变色颜色设置：“我需要你使用seaborn中的barplot语句进行绘制，注意要使用误差棒标记标准差，在图中可视化显示，同时注意需要你在保存图片的时候设置dpi=150”

**仍然存在的问题**
- capsize未进行设置
- 保存图片的名字未进行设置
- 输出的代码老版本的seaborn不兼容

**再次prompt+手动处理**
>手动处理：pip install seaborn更新seaborn
>再次提问：“注意水平条形图条形宽度要设置成0.3，并且注意保存的图片名称设置为'route_stops.png'”

**仍然存在图例比例的问题**
- x轴图例端点值过大，导致数据覆盖面过小，挤在Y轴附近

**手动解决**
>手动调整x轴图例缩放比例

**结果**
成功解决可视化问题，可视化方案符合题目的所有要求

### 事例3：使用不再规定范围的pathlib库函数

**初代prompt**
> 提问：“需要你生成一段查找前面已分离出的20条线路的出现过的车辆编号和车辆驾驶员编号的对应关系的代码，保存为txt文件并放到文件夹'路线驾驶员信息'中”

**存在的问题**
- 使用了规定之外的库pathlib，并未使用os库进行操作

**再次prompt**
>“不可以使用pathlib，使用os库进行操作，要求同上，注意代码的兼容性”

**结果**
成功使用os库解决问题，并将路线驾驶员信息成功保存到文件夹中

### 事例4：生成热力图不能清晰反映绩效规律
**初代prompt**
>提问：构造一个 4×10 的热力图,行为上面要求的4个维度，列对应top10，要求使用annot=True 在每格中标注数值，colormap使用 "YlOrRd" 或 "RdYlGn"

**存在的问题**
- 每一个热力图的格子中，只有对应的服务人次，并没有给出具体是那一条线路

**第二代prompt**
>提问：“注意，热力图的每一个格子中，每个格子要标记所在维度的编号，在服务人次的下面，其他不改变”

**结果**
成功生成存在编号的热力图

## 3.Debug 记录

### 错误1：Task1 数据预处理中数据分隔问题

**错误现象**：
>AI生成的代码中，csv文件的分割开始是使用'\t'进行分隔，因此读取出的图例与数据，是以行为单位被放在一个字符串中，在后续进行读取的时候会产生报错 KeyError，无法查询到“交易时间”这个数据集，无法摘出hour

**原因查找过程**
1. 询问AI出现错误的原因，在一开始AI给出的答复是不存在“交易时间”这个图例，但显然与事实不符。
2. 告诉AI存在“交易时间”这个图例之后，要求它给出其他的错误可能
3. AI仍然无法给出正确的答复，而是将问题转向csv读取指令存在问题或者文件读取数据中存在NaT的错误形式
4. 让AI给出查询问题的解决方式，AI提出可以在打印csv前五行数据的同时将图例也打印出来

**解决优化**
>打印后发现所有图例被存在一个字符串中，因此发现sep的设置错误，将'\t'更改为','后成功输出

### 错误2：高峰小时 resample 格式收入错误与最大窗口时间显示错误

**报错现象**：
- 使用 `resample('5T').sum()` 后，打印出的最大5分钟窗口起始时间为 `08:00`，但实际数据中高峰小时为8点，担心窗口可能跨到9点。
- 版本不支持'T'的查询，需要改成min

**解决过程**：
- 确认 `resample` 默认以索引时间对齐，且窗口左闭右开。由于我们只提取了高峰小时内的数据，窗口不会跨小时。为了更精确，可指定 `closed='left', label='left'`，但默认行为已满足要求。
- 将T 改成min，不再存在valueerror
- 最终保留默认设置，输出正确的时间窗口。

